name: Release Request

on:
  pull_request:
    paths:
      - .github/releases/**
      - scripts/test-release-request.sh
      - scripts/validate-release-request.sh
      - migration/final-python-line.json
      - scripts/validate-final-python-line.sh
      - .github/workflows/release-request.yml
  push:
    branches:
      - main
    paths:
      - .github/releases/**
      - migration/final-python-line.json

permissions:
  contents: read

concurrency:
  group: release-request-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate:
    name: Validate immutable release request
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      request_present: ${{ steps.request.outputs.request_present }}
      release_tag: ${{ steps.request.outputs.release_tag }}
      target_sha: ${{ steps.request.outputs.target_sha }}
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Validate request diff and exact target
        id: request
        shell: bash
        env:
          BEFORE_SHA: ${{ github.event.before }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail

          if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            base="$PR_BASE_SHA"
            head="$PR_HEAD_SHA"
            required=false
          else
            base="$BEFORE_SHA"
            head="$GITHUB_SHA"
            required=true
          fi

          changes=()
          mapfile -t changes < <(
            git diff --name-status "$base" "$head" -- .github/releases/
          )
          if (( ${#changes[@]} == 0 )); then
            if [[ "$required" == "true" ]]; then
              marker_changed="$(
                git diff --name-only "$base" "$head" -- migration/final-python-line.json
              )"
              if [[ -z "$marker_changed" ]]; then
                echo "::error::a release-triggering main push must add one request"
                exit 1
              fi
            fi
            echo "request_present=false" >> "$GITHUB_OUTPUT"
            echo "No native release request changed."
            exit 0
          fi
          if (( ${#changes[@]} != 1 )); then
            echo "::error::a release change must add exactly one immutable request file"
            printf '%s\n' "${changes[@]}"
            exit 1
          fi

          IFS=$'\t' read -r status path extra <<<"${changes[0]}"
          if [[ "$status" != "A" || -n "${extra:-}" ]]; then
            echo "::error::release requests are append-only and cannot be modified, renamed, or deleted"
            exit 1
          fi
          if [[ ! "$path" =~ ^\.github/releases/v[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?\.json$ ]]; then
            echo "::error::invalid release request path: $path"
            exit 1
          fi

          context="$(./scripts/validate-release-request.sh "$path" "$head")"
          {
            echo "request_present=true"
            echo "release_tag=$(jq -r '.tag' <<<"$context")"
            echo "target_sha=$(jq -r '.target' <<<"$context")"
          } >> "$GITHUB_OUTPUT"

      - name: Exercise tamper rejection
        run: ./scripts/test-release-request.sh

  final_python_line:
    name: Validate final Python line marker
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      marker_present: ${{ steps.marker.outputs.marker_present }}
      tag: ${{ steps.marker.outputs.tag }}
      target: ${{ steps.marker.outputs.target }}
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Validate immutable migration marker
        id: marker
        shell: bash
        env:
          BEFORE_SHA: ${{ github.event.before }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            base="$PR_BASE_SHA"
            head="$PR_HEAD_SHA"
          else
            base="$BEFORE_SHA"
            head="$GITHUB_SHA"
          fi

          changes=()
          mapfile -t changes < <(
            git diff --name-status "$base" "$head" -- migration/final-python-line.json
          )
          if (( ${#changes[@]} == 0 )); then
            echo "marker_present=false" >> "$GITHUB_OUTPUT"
            echo "No final Python line marker changed."
            exit 0
          fi
          if (( ${#changes[@]} != 1 )); then
            echo "::error::the final Python line marker must be one immutable file"
            exit 1
          fi
          IFS=$'\t' read -r status path extra <<<"${changes[0]}"
          if [[ "$status" != "A" || "$path" != "migration/final-python-line.json" || -n "${extra:-}" ]]; then
            echo "::error::the final Python line marker is append-only"
            exit 1
          fi

          context="$(./scripts/validate-final-python-line.sh "$path" "$head")"
          {
            echo "marker_present=true"
            echo "tag=$(jq -r '.tag' <<<"$context")"
            echo "target=$(jq -r '.target' <<<"$context")"
          } >> "$GITHUB_OUTPUT"

  authorize:
    name: Authorize tag and dispatch exact release
    needs: validate
    if: >-
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.validate.outputs.request_present == 'true'
    runs-on: ubuntu-24.04
    permissions:
      actions: write
      contents: write
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          fetch-depth: 0

      - name: Refuse an already-published release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ needs.validate.outputs.release_tag }}
        run: |
          if gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "::error::release $RELEASE_TAG already exists and is immutable"
            exit 1
          fi

      - name: Create or verify the lightweight tag
        shell: bash
        env:
          RELEASE_TAG: ${{ needs.validate.outputs.release_tag }}
          TARGET_SHA: ${{ needs.validate.outputs.target_sha }}
        run: |
          set -euo pipefail
          remote_tag="$(
            git ls-remote --refs --tags origin "refs/tags/$RELEASE_TAG" |
              awk 'NR == 1 { print $1 }'
          )"
          if [[ -n "$remote_tag" ]]; then
            git fetch --force origin "refs/tags/$RELEASE_TAG:refs/tags/$RELEASE_TAG"
            existing="$(git rev-parse "refs/tags/$RELEASE_TAG^{commit}")"
            if [[ "$existing" != "$TARGET_SHA" ]]; then
              echo "::error::existing $RELEASE_TAG points to $existing, expected $TARGET_SHA"
              exit 1
            fi
            echo "$RELEASE_TAG already points to the requested target."
          else
            git push origin "$TARGET_SHA:refs/tags/$RELEASE_TAG"
          fi

      - name: Dispatch the release workflow at the tag
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ needs.validate.outputs.release_tag }}
        run: |
          gh workflow run rust-release.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref "$RELEASE_TAG"
          {
            echo "### Release dispatched"
            echo
            echo "The native release workflow was dispatched at \`$RELEASE_TAG\`."
            echo "Its workflow context is therefore bound to the tagged source commit."
          } >> "$GITHUB_STEP_SUMMARY"

  tag_final_python_line:
    name: Create immutable final Python tag
    needs: final_python_line
    if: >-
      github.event_name == 'push' &&
      github.ref == 'refs/heads/main' &&
      needs.final_python_line.outputs.marker_present == 'true'
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
        with:
          fetch-depth: 0

      - name: Create or verify the annotated migration tag
        shell: bash
        env:
          FINAL_TAG: ${{ needs.final_python_line.outputs.tag }}
          FINAL_TARGET: ${{ needs.final_python_line.outputs.target }}
        run: |
          set -euo pipefail
          remote_tag="$(
            git ls-remote --refs --tags origin "refs/tags/$FINAL_TAG" |
              awk 'NR == 1 { print $1 }'
          )"
          if [[ -n "$remote_tag" ]]; then
            git fetch --force origin "refs/tags/$FINAL_TAG:refs/tags/$FINAL_TAG"
            existing="$(git rev-parse "refs/tags/$FINAL_TAG^{commit}")"
            if [[ "$existing" != "$FINAL_TARGET" ]]; then
              echo "::error::existing $FINAL_TAG points to $existing, expected $FINAL_TARGET"
              exit 1
            fi
            echo "$FINAL_TAG already points to the reviewed final Python commit."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git tag --annotate "$FINAL_TAG" "$FINAL_TARGET" \
              --message "Final archived Python implementation before the Rust-only cutover"
            git push origin "refs/tags/$FINAL_TAG"
          fi
